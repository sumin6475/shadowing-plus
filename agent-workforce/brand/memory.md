# Brand — Memory

*Fixed brand decisions only (written when you bless them). Style recipes live in `style.md`; winning assets live in `examples/`.*

## Locked decisions
*(none yet — e.g. "Landing hero pattern = X", "We use [tool] for product video")*

## Things we tried and rejected (so we don't repeat them)
*(none yet — e.g. "carousel skill: too generic, dropped")*
