# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Mobile product context

Before making product, UX, content-model, naming, or AI-recommendation decisions,
read `docs/product/speaking-world.md`. Treat it as the current product direction
for the mobile app unless the user explicitly revises it.
