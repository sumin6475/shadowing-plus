# OS
.DS_Store

# Media files (keep folder structure)
media/*.mp4
media/*.mkv
media/*.avi
media/*.mov
media/*.webm
media/*.wav
media/*.mp3

# Python
scripts/.env
scripts/__pycache__/

# Vibe app data (models, logs, config)
model/

# Node.js
web/node_modules/
web/.next/
web/.env.local

# Debug
debug_*.json
.vercel

# Docs (local only)
ARCHITECTURE.md
TRANSLATE_STAGE_SNAPSHOT.md

# Launch / business docs (local only)
docs/launch/

# Editor
.obsidian/

# Scratch
test.ts

# Build System (ai-layer) — hooks + loop artifacts
__pycache__/
e2e-screenshots/
.claude/claude-md-review.md
.claude/.claude-md-review-state
.env*

# Build Journal hook transients
# Build Journal is local evidence and must never be committed.
docs/journal/
docs/journal/.pending.md
.claude/.journal-gate-state

# Unreferenced design drafts (~11 MB). Kept locally, never committed:
# logo/icon explorations that no code imports. Ship only the chosen asset.
apps/mobile/assets/icon-concepts/
web/public/icons/saylo-*.png
